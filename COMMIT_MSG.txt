P9-A2: org-unit/membership repository + admin endpoints

- database/src/org-admin-repository.ts: workspace-scoped OrgAdminRepository,
  moveOrgUnit re-validates domain cycle/cross-workspace guards before write,
  assignMembership re-checks unit+membership in workspace, feature-setting
  upserts into workspace_settings (feature:<key>)
- worker/src/routes/org-admin-api.ts: permission-scoped admin routes
  (org-unit tree/move, membership assign, feature read/write), narrow
  OrgAdminGateway for testability, fail-closed feature keys
- tests: 15 focused (7 repository + 8 route); suite 71 files / 408 tests
- ROADMAP: P9-A2 closure, P9-A3 -> NEXT
