{
  "name": "fieldrep-os",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev:web": "pnpm --filter @fieldrep/web dev",
    "dev:worker": "pnpm --filter @fieldrep/worker dev",
    "validate:migrations": "node scripts/validate-migrations.mjs",
    "validate:pwa": "node scripts/validate-pwa.mjs",
    "typecheck": "tsc -p packages/domain/tsconfig.json --noEmit && tsc -p packages/shared/tsconfig.json --noEmit && tsc -p packages/permissions/tsconfig.json --noEmit && tsc -p packages/database/tsconfig.json --noEmit && tsc -p packages/ui/tsconfig.json --noEmit && tsc -p apps/web/tsconfig.json --noEmit && tsc -p apps/worker/tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "pnpm --filter @fieldrep/web build && pnpm --filter @fieldrep/worker build",
    "check": "pnpm validate:migrations && pnpm validate:pwa && pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
